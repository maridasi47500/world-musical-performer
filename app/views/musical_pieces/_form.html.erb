<%= form_with(model: musical_piece) do |form| %>
  <% if musical_piece.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(musical_piece.errors.count, "error") %> prohibited this musical_piece from being saved:</h2>

      <ul>
        <% musical_piece.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
